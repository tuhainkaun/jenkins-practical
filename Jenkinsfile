pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo "Build successful"
            }
        }

        stage('Deploy') {
            steps {
                echo "Deploy successful"
            }
        }
    }
}
